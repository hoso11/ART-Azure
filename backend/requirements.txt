# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0

# Validation & Settings
pydantic[email]==2.10.3
pydantic-settings==2.7.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# Task queue
celery[redis]==5.4.0
redis==5.2.1

# Storage
minio==7.2.12
# Azure Blob Storage SDK — used when STORAGE_BACKEND=azure (Azure deploy).
# MinIO stays for local docker-compose dev.
azure-storage-blob==12.23.1
# Azure Identity — DefaultAzureCredential for System-Assigned Managed Identity
# auth against the storage account (Task C2a). Falls back to env-var / VS
# Code / Azure CLI credentials in local dev.
azure-identity==1.19.0

# Email
aiosmtplib==3.0.2

# Logging
loguru==0.7.3

# Image processing
Pillow==11.0.0

# HTTP
httpx==0.28.1

# Rate limiting (in-process; per-Uvicorn-worker counters — acceptable for F1)
slowapi==0.1.9

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1

# Utilities
python-multipart==0.0.18
